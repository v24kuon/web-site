export type NewsStateType = {
  id: number;
  url: string;
  date: string;
  title: string;
}[];

export type PostStateType = {
  postLink: string;
  postTitle: string;
  postImage: string;
};

export type RemodelingStateType = {
  id: number;
  url: string;
  title: string;
  thumbnail: string;
}[];
